{
  "name": "@liquidator/note-monitor",
  "version": "0.1.0",
  "description": "Note monitoring service for tracking escrow positions",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "build": "bun build src/index.ts --outdir=dist --target=bun"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.7",
    "@liquidator/shared": "workspace:*",
    "hono": "^4.6.15",
    "pino": "^9.6.0",
    "pino-pretty": "^13.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
