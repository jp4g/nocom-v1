# Nocom Smart Contracts

## Scripts
Build all contracts and artifacts: `bun run build:nr`
Test the private escrow/ lending pool in TXE: `aztec test`
Test the e2e js integration: `bun test`
Deploy to sandbox: `bun run deploy`
Deploy to sandbox with populated markets: `bun run deploy -p`